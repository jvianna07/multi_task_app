name: multi_task_app
description: "A multi calculator application."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  expressions: ^0.2.5+3
  flutter:
    sdk: flutter
  transparent_image: ^2.0.1
  flutter_launcher_icons: ^0.14.4
  http: ^1.6.0
  intl: ^0.20.2
  country_flags: ^4.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/icon.png"
